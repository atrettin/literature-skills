"""Search published scientific papers, read them, and cite what they say.

The modules here do the work that the `literature` skills describe. Each one
that a skill or an agent runs is reachable as a subcommand of `litdb`; see
`lit/__main__.py` for the list. The rest are libraries those subcommands share.
"""
